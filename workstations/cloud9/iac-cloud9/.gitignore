# Terraform temporary files and directories
.terraform/          # Directory where Terraform stores downloaded modules and plugins
*.tfstate.backup     # Backup of the state file created during specific operations

# Configuration files
*.tfvars             # Variable definition files that often contain sensitive data (e.g., passwords, keys)
*.tfvars.json        # JSON version of variable definition files

# Crash log files
crash.log            # Log file created when Terraform crashes

# Lock file (managed by Terraform)
.terraform.lock.hcl  # Dependency lock file (optional to commit, but generally recommended to track for consistent dependency versions)

# Secrets (if not using .tfvars)
# Add specific files containing secrets that are not covered by the patterns above
# e.g., secrets.auto.tfvars

# Miscellaneous
# Add any other files or directories specific to your project or OS
